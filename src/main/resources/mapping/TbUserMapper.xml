<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.txzmap.spliceservice.mapper.TbUserMapper">


    <select id="get" resultType="com.txzmap.spliceservice.entity.TbUser" >
        select * from tb_user where userName=#{userName} and password=#{password}
    </select>


    <select id="insert" parameterType="com.txzmap.spliceservice.entity.TbUser">
       insert  into tb_user (userName, password, expiration, createTime) VALUES (#{userName},#{password},#{expiration},#{createTime})
    </select>


    <select id="changePassword">
       update  tb_user set password=#{password} where userName=#{userName}
    </select>


</mapper>
